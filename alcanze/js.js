

    const x = 1;

    function a() {
        output(z);
      }
      
      function b() {
        output(y);
      }


    function output(value) {
              const para = document.createElement('p');
    document.body.appendChild(para);
    para.textContent = `Value: ${value}`;
          }
      alert('HELLO BIG NIGGA' );
 addEventListener ( 'keydown','button', onKeyDown );   
 function onKeyDown(e) {
    if (e.keyCode === 13) {
      alert('Hello');
    }
  } 